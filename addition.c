#include<stdio.h>
int main()
    {
      int a, b ,c;
      printf("enter the value of a and b");
      scanf("%d%d", &a, &b);
      printf("addition of two number is %d", c);
      return 0;
    }
